name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: deploy
      run: curl -u 'deploy:4&2Fh*#1Fg5~%3E' https://greenit.site/deploy/deploy.php
